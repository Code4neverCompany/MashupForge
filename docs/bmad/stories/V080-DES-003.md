# STORY: V080-DES-003 — Settings Tag Sync + AI Prompt Binding

## Status: Open
## Assignee: designer
## Brief: V080-POLISH

## Summary
Two related improvements:
1. Tags selected during onboarding should be editable in Settings (niches/genres section)
2. The AI system prompt should dynamically incorporate the user's selected tags, niches, and genres

## Acceptance Criteria
- [ ] SettingsModal/DesktopSettingsPanel shows current niches and genres with add/remove controls
- [ ] Changes in Settings persist and affect AI generation
- [ ] AI system prompt dynamically includes user's niches, genres, and custom tags
- [ ] AI output is visibly influenced by the configured focus areas
- [ ] The system prompt is not a static string — it rebuilds when settings change

## Technical Notes
- SettingsModal.tsx line 1041: static system prompt string
- SettingsModal.tsx lines 956-957: niches/genres already stored in settings as `agentNiches` / `agentGenres`
- The prompt should interpolate these: "You specialize in {niches.join(', ')} with focus on {genres.join(', ')}"
- Settings UI already has some niche/genre display (line 1005) — extend with edit controls
- DesktopSettingsPanel.tsx may need the same treatment
- The AI engine (pi.dev) receives the prompt — trace how it's passed to ensure dynamic prompt reaches the model

## Files
- components/SettingsModal.tsx
- components/DesktopSettingsPanel.tsx
- lib/ai-engine.ts or similar (prompt delivery)
