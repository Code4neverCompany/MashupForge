# STORY: V080-DES-001 — Onboarding Scroll & Tag Expansion

## Status: Open
## Assignee: designer
## Brief: V080-POLISH

## Summary
Two onboarding issues:
1. The Continue/Next button at the bottom of the wizard becomes unreachable when many tags are selected (content pushes buttons below the visible area)
2. Only ~2 tags can be selected from a pool of 30+ — should allow selecting many more

## Acceptance Criteria
- [ ] Continue/Next button is ALWAYS visible and clickable regardless of content height
- [ ] The scrollable content area does not push buttons out of view
- [ ] User can select 10+ tags (no artificial low limit)
- [ ] Tag pool expanded to 30+ recommended tags across niches and genres
- [ ] Selected tags are visually distinct from unselected
- [ ] Scroll works smoothly within the tag area

## Technical Notes
- OnboardingWizard.tsx line 143: container has `overflow-hidden`, line 163: content has `overflow-y-auto`
- The buttons (lines 195-231) are OUTSIDE the scrollable area but may be pushed by the flex layout
- Fix: make the button bar `sticky bottom-0` or use `flex-shrink-0` to prevent it from collapsing
- Step2Niche.tsx: expand the tag arrays, ensure multi-select works (check `onChangeUniverses` / `onChangeGenres` callbacks)
- The wizard uses `min-h-[540px]` — may need to allow the modal to grow taller or ensure scroll works within the fixed height

## Files
- components/onboarding/OnboardingWizard.tsx
- components/onboarding/steps/Step2Niche.tsx
