# QA Review — FIX-101

**Status:** PASS
**Agent:** QA (Quinn)
**Date:** 2026-04-15
**Commit:** 9300ce1

## Findings

### `app/api/social/post/route.ts` — `waitForIgContainerReady()`

- [INFO] Replaces 2× `await new Promise(resolve => setTimeout(resolve, 5000))` (blind 5s + 5s = 10s fixed sleep) with a polling loop. Correct approach for a status-gated API operation.
- [INFO] Polls `GET /{containerId}?fields=status_code` every 1500ms, max 60s (40 attempts). 60s ceiling is generous but appropriate — Instagram can be slow for carousel assembly.
- [INFO] Terminal states handled:
  - `FINISHED` / `PUBLISHED` → return (success). ✓
  - `ERROR` / `EXPIRED` → throw with descriptive message. ✓
  - Loop exhaustion → throw `'Instagram container not ready after 60s'`. ✓
- [INFO] `IN_PROGRESS` / unknown states → continue polling. Correct — don't fail-fast on transient states.
- [INFO] Applied to both single-image publish path and carousel publish path — consistent treatment. ✓
- [INFO] No user-supplied input in the URL construction (`containerId` comes from a prior IG API response, not direct user input). No injection surface. ✓

### Regression analysis
- [INFO] Previous 10s fixed sleep was both too slow (happy path) and too fast (cold carousel assembly). Polling is strictly better on both axes.
- [INFO] 60s timeout is a hard ceiling — no possibility of a hung publish blocking the route indefinitely. ✓

### Scope
- [INFO] +41 / -2 lines. Isolated to `post/route.ts`. No type shape changes, no new dependencies.

## Gate Decision

PASS — Polling helper is correct. All terminal states handled. 60s hard ceiling prevents hung publishes. No injection surface. Strictly better than the prior fixed sleep.
