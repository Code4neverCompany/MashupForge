# QA Gate: V030-QA-003 — Pipeline log IDB persistence + weekly fill logic

**Date:** 2026-04-18
**Reviewer:** QA subagent
**Status:** PASS

---

## What was done

Both test files were already partially authored when this gate ran (8 tests each,
230/230). Reviewed coverage, identified gaps, and added 4 targeted tests:
- `tests/lib/pipeline-log-store.test.ts`: +2 (empty-save no corruption, exact-50 boundary)
- `tests/lib/weekly-fill.test.ts`: +2 (undefined posts, ts===now boundary)

Final count: 234/234. tsc clean.

---

## Acceptance criteria

| Criterion | Result |
|---|---|
| Pipeline log IDB save/hydrate/clear tests | ✅ — 10 tests: roundtrip, Date↔ISO, empty, clear, trim-to-50, exact-50, empty-save, 3× IDB error swallow, non-array stored value |
| Weekly fill gap detection tests (V030-004) | ✅ — 10 tests: empty, status filter, past exclusion, window boundary, filled/percent, over-target cap, postsPerDay, malformed date, targetDays=0, undefined posts, exact-now boundary |
| All tests pass (223+ existing + new) | ✅ — 234/234 |
| Write to inbox when done | ✅ |

---

## Test cases

### `tests/lib/pipeline-log-store.test.ts` — 10 tests

| Test | What it covers |
|---|---|
| saves then loads with Date fields intact | Full roundtrip; timestamp is `instanceof Date` after load |
| returns [] when nothing saved | Cold start behavior |
| clearPipelineLog wipes so load returns [] | Clear is effective |
| trims to last 50 when input > 50 | Oldest entries dropped, newest kept |
| entries at exactly 50 stored without trimming | Boundary: `> 50` not `>= 50` |
| saving [] produces empty load (no corruption) | Empty-write doesn't leave stale data |
| savePipelineLog swallows IDB errors | best-effort, does not throw on `set` failure |
| loadPipelineLog swallows IDB errors → returns [] | Resilience on `get` failure |
| clearPipelineLog swallows IDB errors | best-effort on `del` failure |
| loadPipelineLog handles non-array stored value | Corrupt store returns [] not a throw |

### `tests/lib/weekly-fill.test.ts` — 10 tests

| Test | What it covers |
|---|---|
| empty schedule → 0%, all gaps = postsPerDay | Baseline + dayLabel sequence (Mon..Sun) |
| counts scheduled + pending_approval; ignores posted + failed | Status filter |
| posts before now excluded even on today | Time comparison (09:00 < 12:00 anchor) |
| posts outside target window ignored | Day 7+ excluded |
| filled=true + percent=100 at exact target | Exactly 14 posts across 7 days |
| over-target: filled=true, percent capped at 100, gap=0 | 17 total; percent doesn't exceed 100 |
| respects non-default postsPerDay | `postsPerDay=4` → targetTotal=28, gap=1 |
| malformed date string skipped, no throw | Resilience on bad input |
| targetDays=0 → empty days, percent=0, filled=true (0≥0) | Zero-division guard |
| undefined posts treated as empty | `undefined` input handled |
| post at ts === now is counted (ts < now is false) | Exact-now boundary inclusive |

---

## Notes

- `computeWeekFillStatus` takes `now` as a parameter — tests pin to
  `new Date(2026, 3, 20, 12, 0, 0)` (Monday 2026-04-20 noon local) for
  determinism without fake timers.
- IDB mock pattern (`vi.mock` + dynamic import after mock) is identical to
  `tests/lib/pipeline-checkpoint.test.ts` — consistent approach across all
  IDB-backed stores.
